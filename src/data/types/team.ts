export type Team = {
  slug: string;
  name: string;
  emphasis: string;
  ageRange: string;
  liga: string;
  description: string;
  trainings: string[];
};

// Trainer werden über Assignements zugewiesen
